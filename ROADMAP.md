# Roadmap — CV-Tailoring Extension

The original build (four services, live deployment, initial polish pass) is
done — see [`ROADMAP.old.md`](ROADMAP.old.md) for that history. This roadmap
covers the next piece of work: the CV-tailoring feature in `agent-api`, plus
a short backlog of items carried over from the old roadmap that were still
open when this file was created.

**Design is already settled and documented** — this roadmap tracks
*implementation*, not design. Before writing code for any milestone below,
read:

- [`services/agent-api/docs/cv-tailoring.md`](services/agent-api/docs/cv-tailoring.md) — the feature spec (JSON contract, two-step flow, delivery, rate limiting)
- [`services/agent-api/AGENTS.md`](services/agent-api/AGENTS.md) — the agent's runtime guardrail contract
- [`.claude/skills/cv-tailoring/SKILL.md`](.claude/skills/cv-tailoring/SKILL.md) — the implementation playbook
- [`AGENTS.md`](AGENTS.md) — repo-wide dev conventions

## Milestone 1 — Carried-over backlog

Open items from the old roadmap, unrelated to CV-tailoring but not yet done.
Pick these up opportunistically; they don't block the milestones below.

- [x] Add smoke test step to each service's CI workflow: `curl /health` on
      the deployed app before marking the deploy green
- [x] Chat panel: add a retry button to the existing inline error state
- [x] Dependabot or Renovate for automated dependency updates
- [x] Military service section: add a `/military` endpoint to
      `experience-api`, wire up a frontend section

**Technical debt** (deferred by choice, not forgotten):
- [x] Chat panel loading state is a pulsing "thinking…" bubble, not a real
      skeleton loader — replaced with a proper `animate-pulse` skeleton
      matching the pattern used by every other section
- [x] Accessibility audit: tab order and light-mode colour contrast manually
      verified in a real browser. Found and fixed three real issues along
      the way: `textareaRef`/`messagesEndRef` were shared across the
      mobile+desktop duplicate DOM trees (auto-focus/auto-scroll could
      silently target the CSS-hidden instance), the mobile full-screen chat
      overlay had no focus trap (Tab could escape into page content hidden
      behind it), and three `text-accent-600`/`text-zinc-400` usages failed
      WCAG AA contrast in light mode

## Milestone 2 — experience-api: achievement metadata

- [x] Add `Achievement` record (`Text`, `Tags: string[]`, `Metric: string?`)
      and a new nullable `Achievements` array on `ExperienceEntry`,
      alongside the untouched `Highlights`/`Stack`
- [x] Backfill `achievements` for all 5 existing roles in `cv.json`,
      `text` mirroring the corresponding `highlights` entry
- [x] Strip `achievements` from what the existing `get_work_experience` tool
      forwards to Claude — its 8000-char budget and behavior must not change
- [x] Test: `achievements[].text` set matches `highlights[]` set, per role
- [x] Bump `Service.Version` in `appsettings.json`

**Acceptance:** existing `/api/v1/experience` consumers (frontend,
`get_work_experience`) are byte-identical in behavior; new field is present
and only reachable by new code.

## Milestone 3 — agent-api: tailor step

- [x] New `app/cv_tailor.py`: `TailoredHighlight` / `TailoredRole` /
      `TailoredCV` pydantic contract, `TAILOR_SYSTEM_PROMPT`, second
      independent Claude call
- [x] `job_description` wrapped in `<job_description>` tags with an explicit
      untrusted-data instruction; `JOB_DESCRIPTION_MAX_CHARS` bound enforced in
      code now (the tool's `input_schema` `maxLength` lands with M5's registration)
- [x] Mechanical validation, all four gates in order: existence,
      text-fidelity (`TEXT_FIDELITY_THRESHOLD = 0.6`), numeric-fidelity,
      structural cap (`MAX_HIGHLIGHTS_PER_ROLE = 4`, `MAX_TOTAL_HIGHLIGHTS = 12`)
- [x] Test: all four gates, independently and in combination — this is the
      feature's actual correctness surface
- [x] Test: `TAILOR_SYSTEM_PROMPT` guardrail-phrase assertions (mirrors
      `TestSystemPrompt`)

**Acceptance:** a tailor response that cites a real source but distorts,
inflates, or over-collects it is rejected before it ever reaches render.

## Milestone 4 — agent-api: render step

- [x] Scaffold `app/templates/cv_template.docx` — placeholder styling,
      fields bound 1:1 to the `TailoredCV` contract (regenerated by the
      checked-in `app/templates/build_cv_template.py`)
- [x] `docxtpl` render to a `secrets.token_urlsafe(32)`-named file in `/tmp`
      (`app/cv_render.py`; StrictUndefined + autoescape)
- [x] Test: docxtpl render regression against the real template file
      (catches placeholder/field-name drift)

**Acceptance:** a valid `TailoredCV` renders to a real, openable `.docx`.

## Milestone 5 — agent-api: delivery & rate limiting

- [x] `generate_tailored_cv` tool registered in `TOOLS`/`dispatch()`
      (`target_role`, `job_description` both required, empty string allowed)
- [x] In-memory token store; `GET /cv/tailored/{token}` claims atomically
      (`dict.pop` before any `await` via `claim_download`), 404 if already gone or
      expired (`DOWNLOAD_TTL_SECONDS`, default 10 min, lazy expiry check)
- [x] File cleanup via `BackgroundTask` after response streams
- [x] Tool-specific sliding-window rate limit: `TAILOR_RATE_LIMIT`
      (default 3/hour per IP), independent of `/chat`'s existing `slowapi` limit
- [x] Route-level `slowapi` limit on the download endpoint (20/hour per IP)
- [x] `ChatResponse.download_url` field, populated from the tool result
      (raw token never fed back to Claude)
- [x] Confirm `agent-api`'s Container App is pinned to `maxReplicas: 1` in
      `infra/terraform/` — **was `2`, corrected to `1` for agent-api only**
- [x] Bump `SERVICE_VERSION` in `main.py` (1.0.2 → 1.1.0)
- [x] Test: token lifecycle (single fetch, second fetch 404s, expired fetch
      404s and cleans up, single-claim race)
- [x] Test: sliding-window rate limiter with mocked `time.time()`
- [x] Test: `dispatch()` orchestration with mocked Claude/render calls —
      success path returns `download_token`; any gate failure or
      experience-api outage returns a clean `{"error": ...}`

**Acceptance:** a visitor can request and download exactly one tailored CV
per successful tool call; nothing is left behind past the TTL under normal
operation.

## Milestone 6 — frontend wiring

- [x] `ChatPanel.tsx`: render `download_url` as a plain `<a href download>`
      against `API.agent` when present on a response — no blob-fetch code needed
- [x] Test (or manual check): multi-turn slot-filling — turn 1 with missing
      `job_description` gets a clarifying reply, not a tool call; turn 2,
      with turn 1 in history, successfully calls the tool. Verified live against
      the running stack (vague request → clarifying question, no tool call;
      full request → `generate_tailored_cv` → working download link)

**Acceptance:** an HR visitor can ask for a tailored CV in conversation,
get asked for a job description if they didn't give one, and receive a
working download link in the same panel.

## Milestone 7 — ship it

- [ ] `uv add docxtpl` (or equivalent) in `agent-api`'s `pyproject.toml`
- [ ] Full test suite green, `docker compose up` manual smoke test of the
      whole flow end to end
- [ ] Update `CLAUDE.md` if a new convention is worth codifying (per-tool
      rate limiting pattern, `/tmp`-backed token store) — see the "flagged
      for later pass" notes in `cv-tailoring.md`
- [ ] Confirm root `README.md` needs no update (architecture shape is
      unchanged — still 3 services, no new service)
- [ ] Deploy via existing `agent-api.yml` / `experience-api.yml` CI

**Acceptance:** the feature is live, documented in `CLAUDE.md` where it
introduces a new convention, and the old roadmap's backlog (Milestone 1
above) is either done or explicitly re-carried into whatever roadmap comes
after this one.

---

## Working with Claude Code on this repo

Always read [`CLAUDE.md`](./CLAUDE.md) at the start of a session. It encodes
the architectural rules. When asking Claude Code to add a feature:

- Identify which service it belongs to first
- Don't let it add shared code between services
- Don't let it add a database, queue, or cache
- Bump the relevant service's version when changing behavior
- Ask it to update the relevant `README.md` if conventions change

The most common failure mode will be Claude Code suggesting "let me extract
this into a shared library" — say no every time. Duplication is a feature
here.
